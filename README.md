# prishaugc.io

## GitHub Pages

This repository now contains a GitHub Actions workflow that builds the `prisha-ugc-portfolio` app and deploys the generated `dist/` to GitHub Pages on push to `main`.

The workflow file is at `.github/workflows/deploy.yml`.